import { AuthForm } from '../../../feature/auth/ui/AuthForm';

import './LoginPage.scss';

const LoginPage = () => (
    <div className="login-container">
        <AuthForm />
    </div>
);

export default LoginPage;
