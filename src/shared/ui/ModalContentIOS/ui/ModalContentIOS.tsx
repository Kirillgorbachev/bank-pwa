import { ContentDownloadIOS } from '@/shared/ui/ConentDownloadIOS';
import { ModalContent } from '@/shared/ui/ModalContentIOS/wrapper/ModalContentWrapper';

export const ModalContentIOS = () => <ModalContent content={<ContentDownloadIOS />} modalStyleType="long" />;
